import { gql, useQuery } from '@apollo/client'

// const LIST_ALL_DICE_EFFECTS = gql`
//   query AllDiceEffects() {
//   }
// `

export default function useAllDiceEffects(idList: string[]) {
  return 1
}
